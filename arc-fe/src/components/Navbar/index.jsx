import React, { useState } from 'react';
import {Layout} from 'antd';
const { Header} = Layout;

const Navbar = () => {
    return (

        <Header
            style={{
                backgroundColor: '#002140',
                padding: 0,
            }}
        />
    );
};
export default Navbar;